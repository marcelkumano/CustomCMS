@inherits Umbraco.Web.Mvc.UmbracoTemplatePage<ContentModels.UnidadesType>
@using ContentModels = Umbraco.Web.PublishedContentModels;
@{
    Layout = "MasterTemplate.cshtml";
}

@* the fun starts here *@

<div class="container">

    <div class="starter-template">
        <h1>@Umbraco.Field("titulo")</h1>
        <p class="lead">@Umbraco.Field("descricao")</p>
    </div>


    @{
        var selection = Model.Content.Site().FirstChild("unidadesType").Children("unidadeType")
                            .Where(x => x.IsVisible());
    }

    <div class="row">
        @foreach (UnidadeType item in selection)
        {
            <div class="col-md-4">
                <h2>@item.NomeUnidade</h2>
                <p>@item.DescricaoUnidade</p>
                <p><a class="btn btn-default" href="@item.Url" role="button">View details »</a></p>
            </div>
        }
    </div>
</div>
